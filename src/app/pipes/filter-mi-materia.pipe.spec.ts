import { FilterMiMateriaPipe } from './filter-mi-materia.pipe';

describe('FilterMiMateriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMiMateriaPipe();
    expect(pipe).toBeTruthy();
  });
});
