export interface EventoI {
    id_evento: number,
    fecha: string,
    id_actividad: number, 
    id_estudiante: number,
    check_download: number,
    check_inicio: number,
    check_fin: number,
    check_answer: number,
    count_video: number,
    check_video: number
}